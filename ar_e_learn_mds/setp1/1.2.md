# Step 1.2 — Backend: package.json, tsconfig, ESLint

**লক্ষ্য:** Backend workspace-এর জন্য dependency লিস্ট আর tooling config বসানো, এবং `npm install` চালিয়ে নিশ্চিত হওয়া যে কোনো ভার্সন কনফ্লিক্ট নেই।

> ⚠️ এই package.json-এর প্রতিটা ভার্সন npm registry থেকে verify করা (কোনোটা অনুমান করে বসানো না)। বিশেষভাবে খেয়াল রাখা হয়েছে:
>
> - TypeScript **6.0.3** ব্যবহার করা হয়েছে, latest 7.0.2 না — কারণ `typescript-eslint` এখনো TS7 সাপোর্ট করে না।
> - ESLint **10.8.0** + `eslint-plugin-import-x` (মূল `eslint-plugin-import` ESLint 10 সাপোর্ট করে না)।

## ফাইল তৈরি করুন (backend/ ফোল্ডারের ভেতরে)

**`backend/package.json`**

```json
{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "lint": "eslint . --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "express": "5.2.1",
    "mongoose": "9.9.1",
    "cors": "2.8.6",
    "helmet": "8.3.0",
    "express-rate-limit": "8.6.1",
    "compression": "1.8.1",
    "cookie-parser": "1.4.7",
    "dotenv": "17.4.2",
    "zod": "4.4.3",
    "jsonwebtoken": "9.0.3",
    "jwks-rsa": "4.1.0",
    "@supabase/supabase-js": "2.111.0",
    "pino": "10.3.1",
    "pino-http": "11.0.0"
  },
  "devDependencies": {
    "typescript": "6.0.3",
    "tsx": "4.23.1",
    "pino-pretty": "13.1.1",
    "@types/node": "20.19.43",
    "@types/express": "5.0.6",
    "@types/cors": "2.8.19",
    "@types/jsonwebtoken": "9.0.10",
    "@types/cookie-parser": "1.4.10",
    "@types/compression": "1.8.1",
    "eslint": "10.8.0",
    "@eslint/js": "10.0.1",
    "typescript-eslint": "8.65.0",
    "eslint-config-prettier": "10.1.8",
    "eslint-plugin-import-x": "4.17.1",
    "vitest": "4.1.10"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
```

**`backend/tsconfig.json`**

```json
{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "target": "ES2023",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "lib": ["ES2023"],
    "outDir": "dist",
    "rootDir": "src",
    "declaration": false,
    "sourceMap": true
  },
  "include": ["src"],
  "exclude": ["node_modules", "dist"]
}
```

**`backend/eslint.config.js`**

```javascript
import js from "@eslint/js";
import tseslint from "typescript-eslint";
import importPlugin from "eslint-plugin-import-x";
import prettierConfig from "eslint-config-prettier";
import { fileURLToPath } from "node:url";
import path from "node:path";

const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);

export default tseslint.config(
  js.configs.recommended,
  ...tseslint.configs.recommendedTypeChecked,
  {
    languageOptions: {
      parserOptions: {
        projectService: true,
        tsconfigRootDir: __dirname,
      },
    },
  },
  {
    plugins: { "import-x": importPlugin },
    rules: {
      "import-x/order": [
        "warn",
        {
          groups: [
            "builtin",
            "external",
            "internal",
            "parent",
            "sibling",
            "index",
          ],
          "newlines-between": "always",
          alphabetize: { order: "asc", caseInsensitive: true },
        },
      ],
      "@typescript-eslint/no-unused-vars": [
        "warn",
        { argsIgnorePattern: "^_" },
      ],
      "@typescript-eslint/consistent-type-imports": "warn",
      "no-console": ["warn", { allow: ["warn", "error"] }],
    },
  },
  prettierConfig,
  {
    ignores: ["dist/*", "node_modules/*", "eslint.config.js"],
  },
);
```

**`backend/.env.example`**

```text
# Server
NODE_ENV=development
PORT=4000
CORS_ORIGIN=http://localhost:8081

# MongoDB (Atlas connection string — includes db name at the end of the path)
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/arabic_learning?retryWrites=true&w=majority

# Supabase project (used to verify Auth JWTs issued to the mobile app)
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_JWKS_URI=https://<project-ref>.supabase.co/auth/v1/.well-known/jwks.json
SUPABASE_JWT_AUDIENCE=authenticated
# Only needed as a fallback if the Supabase project still uses legacy HS256 signing.
# Prefer migrating to asymmetric (ES256) keys — see Supabase Dashboard > Project Settings > JWT.
SUPABASE_LEGACY_JWT_SECRET=

# Service-role key — server-side only, never expose to the mobile app.
SUPABASE_SERVICE_ROLE_KEY=

# Rate limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=300

```

## Test করুন

```bash
cd backend
npm install --no-audit --no-fund
```

✅ এক্সপেক্টেড আউটপুট: কোনো `ERESOLVE` এরর ছাড়াই ৩৪০+ প্যাকেজ ইনস্টল হবে।

যদি ERESOLVE এরর আসে, প্রথমে `npm ls eslint` চালিয়ে দেখুন কোন প্যাকেজ কোন ভার্সন চাইছে — সেটা সমাধান না করে এগোবেন না।

## পরের ধাপ

👉 **Step 1.3 — Backend core utilities (env, logger, db, ApiError, ApiResponse)**
